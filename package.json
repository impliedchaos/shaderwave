{
  "name": "shaderwave",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "WebGL2 synthesizer & tracker — instruments synthesized entirely on the GPU.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && ssh dmaez@dataphile \"/usr/bin/rm -rf /home/tug/fun/shaderwave/*\" && scp -r dist/* dmaez@dataphile:/home/tug/fun/shaderwave/"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^6.0.0",
    "vite-plugin-glsl": "^1.6.0"
  }
}
